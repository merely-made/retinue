# Provenance

The source tree is copied byte-for-byte from the crates.io package
`embedded-graphics-core 0.4.1`.

- crates.io checksum:
  `95743bef3ff70fcba3930246c4e6872882bbea0dcc6da2ca860112e0cd4bd09f`
- upstream repository:
  <https://github.com/embedded-graphics/embedded-graphics>
- license: MIT OR Apache-2.0

The local `Cargo.toml` changes the `az` dependency constraint from `~1.2.0` to
`1.3` for the same workspace-resolution reason recorded in the parent
embedded-graphics provenance file. The source code itself is unchanged.
