# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
name = "lora-phy"
version = "3.0.1"
# This is a VENDORED FORK of someone else's crate, pulled in as an implicit
# workspace member through the firmware's path dependency. It must never be
# published from here: the name belongs upstream.
publish = false
description = "A LoRa physical layer implementation enabling utilization of a range of MCU/LoRa board combinations within embedded frameworks supporting embedded-hal-async."
readme = "README.md"
keywords = [
    "lora",
    "radio",
    "embedded-hal-async",
    "iot",
    "semtech",
]
categories = [
    "embedded",
    "no-std",
    "asynchronous",
]
license = "MIT OR Apache-2.0"
repository = "https://github.com/embassy-rs/lora-phy"

[package.metadata.docs.rs]
all-features = true
rustdoc-args = [
    "--cfg",
    "docsrs",
]

[dependencies.defmt]
version = "0.3"

[dependencies.embedded-hal]
version = "1"

[dependencies.embedded-hal-async]
version = "1"

[dependencies.lora-modulation]
version = ">=0.1.2"

[dependencies.lorawan-device]
version = "0.12"
features = ["defmt"]
optional = true

[dependencies.num-traits]
version = "0.2"
default-features = false

[features]
lorawan-radio = ["dep:lorawan-device"]

# Vendored upstream code. Patched only where a defect forces it, never restyled, so its
# lints are not ours to answer. CI runs clippy with -D warnings across the workspace and
# these are path dependencies, which means without this the build fails on somebody else's
# formatting choices. Our own crates are clippy-clean and stay that way.
[lints.rust]
warnings = "allow"

[lints.clippy]
all = { level = "allow", priority = -1 }
pedantic = { level = "allow", priority = -1 }
